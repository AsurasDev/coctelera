# Directus backend URL (no trailing slash)
DIRECTUS_URL=http://localhost:8055

# Directus static admin token (generated in Directus admin → Settings → Access Tokens)
DIRECTUS_TOKEN=your-admin-token-here
